<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{asset "built/index.css"}}" />
    <script src="{{asset "built/index.js"}}" defer></script>

    <title>{{meta_title}}</title>

    {{ghost_head}}
    {{!-- Outputs important meta data and settings, should always be in <head> --}}

</head>

<body class="{{body_class}}">
<div class="gh-viewport">

    {{> header}}

    <main class="gh-main">

        <section class="error-page flex flex-col items-center">
            <h1 class="error-page__title">404</h1>
            <p class="error-page__subtitle">{{t "Oops! It seems you've reached a dead end. The page you were looking for has decided to go on a solo adventure."}}</p>
            <a href="{{@site.url}}" class="button">Back to homepage</a>
        </section>

    </main>

</div>

{{!-- <foot> --}}
{{ghost_foot}}
{{!-- Outputs important scripts - should always be included before closing body tag --}}

</body>
</html>